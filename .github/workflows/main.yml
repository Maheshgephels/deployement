name: Build and Deploy

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # Checkout code from GitHub
      - name: Checkout code
        uses: actions/checkout@v3

      # Set up Node.js (skipped for now)
      # - name: Set up Node.js
      #   uses: actions/setup-node@v3
      #   with:
      #     node-version: '20'

      # Cache Node.js modules (skipped for now)
      # - name: Cache Node.js modules
      #   uses: actions/cache@v3
      #   with:
      #     path: ~/.npm
      #     key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
      #     restore-keys: |
      #       ${{ runner.os }}-node-

      # Build frontend (skipped for now)
      # - name: Build frontend
      #   run: |
      #     echo "Installing and building frontend"
      #     cd frontend
      #     npm ci
      #     npm run build
      #   env:
      #     CI: false

      # Install lftp
      - name: Install lftp
        run: sudo apt-get install -y lftp

      # Deploy frontend to CPanel (FTP test)
      - name: Test FTP connection to CPanel
        run: |
          echo "Testing FTP connection"
          lftp -e "
          open ${{ secrets.FTP_SERVER }};
          set ftp:ssl-allow yes;    # Allow SSL/TLS connections
          set ftp:ssl-force yes;    # Force SSL/TLS connection (explicit TLS)
          quit
          " -u ${{ secrets.FTP_USERNAME }},${{ secrets.FTP_PASSWORD }}

      # Build backend and deploy (Optional)
      # - name: Deploy backend
      #   run: |
      #     echo "Deploying backend"
      #     cd backend
      #     npm ci
      #     pm2 restart app-name
