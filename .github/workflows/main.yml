name: Build and Deploy

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # Get the public IP address of GitHub Actions runner
      - name: Get public IP
        run: curl -s https://ifconfig.me

      # Checkout code from GitHub
      - name: Checkout code
        uses: actions/checkout@v3

      # Install lftp
      - name: Install lftp
        run: sudo apt-get install -y lftp

      # Create a test file to upload
      - name: Create a test file
        run: |
          mkdir -p frontend/build
          echo "This is a test file to check FTP connection" > frontend/build/testfile.txt

      # Check current directory
      - name: Check Current Directory
        run: |
          echo "Checking current FTP directory"
          lftp -d -e "
          open ${{ secrets.FTP_SERVER }};
          set ftp:ssl-allow yes;
          set ftp:ssl-force yes;
          set ftp:passive-mode yes;
          pwd;
          quit
          " -u ${{ secrets.FTP_USERNAME }},${{ secrets.FTP_PASSWORD }}

      # List files in public_html
      - name: List files in public_html
        run: |
          echo "Listing files in public_html"
          lftp -d -e "
          open ${{ secrets.FTP_SERVER }};
          set ftp:ssl-allow yes;
          set ftp:ssl-force yes;
          set ftp:passive-mode yes;
          cd /public_html;
          ls;
          quit
          " -u ${{ secrets.FTP_USERNAME }},${{ secrets.FTP_PASSWORD }}

      # Test FTP connection and upload file
      - name: Upload file to CPanel
        run: |
          echo "Testing FTP connection and uploading file"
          lftp -d -e "
          open ${{ secrets.FTP_SERVER }};
          set ftp:ssl-allow yes;
          set ftp:ssl-force yes;
          set ftp:passive-mode yes;
          cd /public_html/app;
          put frontend/build/testfile.txt;
          ls;  # List files after upload
          quit
          " -u ${{ secrets.FTP_USERNAME }},${{ secrets.FTP_PASSWORD }}
